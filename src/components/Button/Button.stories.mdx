import Button from "./Button";
import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs";

<Meta title="Design System/Button" component={Button} />

# Buttons

To trigger an operation

In Landrik UI we have 7 variants of Buttons

- Default
- Primary
- Danger
- Success

`Props` are listed at the end of this page.

[View source](https://github.com/landrik/landrik-ui/tree/main/src/components/Button)

## Imports

```js
import { Button } from "@landrik/ui";
```

## Usage

<Canvas withSource="open">
  <Story name="default">
    <Button>Default Button</Button>
  </Story>
</Canvas>

### Button Sizes

Button comes in three sizes.
By default they are of **_medium_** size

Use the `size` prop to change the size of the `Button`.
You can set the value to `sm`, `md`, `lg`

<Canvas withSource="open">
  <Story
    name="sizes"
    parameters={{
      docs: {
        source: {
          code: `
            <Button size="sm">Small</Button>
            <Button size="md">Medium</Button>
            <Button size="lg">Large</Button>
            `,
        },
      },
    }}
  >
    <div
      style={{
        display: "flex",
        gap: "1rem",
        alignItems: "center",
        flexWrap: "wrap",
      }}
    >
      <Button size="sm">Small</Button>
      <Button size="md">Medium</Button>
      <Button size="lg">Large</Button>
    </div>
  </Story>
</Canvas>

### Button Shapes

Use the shape prop to change the shape of the `Button`. You can set the value to `rounded`, `circle`

<Canvas withSource="open">
  <Story
    name="shapes"
    parameters={{
      docs: {
        source: {
          code: `
            <Button>Default</Button>
            <Button shape="rounded">Rounded</Button>
            `,
        },
      },
    }}
  >
    <div
      style={{
        display: "flex",
        gap: "1rem",
        alignItems: "center",
        flexWrap: "wrap",
      }}
    >
      <Button>Default</Button>
      <Button shape="rounded">circle</Button>
    </div>
  </Story>
</Canvas>

### Button Types

Use the Types prop to change the visual style of the `Button`. You can set the value to `filled`, `outline`, `text`.

<Canvas withSource="open">
  <Story
    name="types"
    parameters={{
      docs: {
        source: {
          code: `
            <Button type="filled">Filled</Button>
            <Button type="outline">Outline</Button>
            <Button type="text">Text</Button>
            `,
        },
      },
    }}
  >
    <div
      style={{
        display: "flex",
        gap: "1rem",
        alignItems: "center",
        flexWrap: "wrap",
      }}
    >
      <Button type="filled">Filled</Button>
      <Button type="outline">Outline</Button>
      <Button type="text">Text</Button>
    </div>
  </Story>
</Canvas>

| State                | Description                                                                                                                                                                                    |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Primary button       | Use a Primary button to inform about the importance of an action. This is the strongest call to action. No more than one primary button should appear on a screen. Examples: Save, Finish, ... |
| Success button       | Use a Success button for reassurance. Use it preferably to indicate a validation, a success, an end of a workflow, ... Examples: Confirm, Save, ...                                            |
| Danger button        | Use a Danger button to indicate a destructive action. Example: Delete                                                                                |


### Button Variants

Use the Types prop to change the visual style of the `Button`. You can set the value to `primary`, `success`, `danger`, `success`.

<Canvas withSource="open">
  <Story
    name="variants"
    parameters={{
      docs: {
        source: {
          code: `
            <Button variant="primary">Primary</Button>
            <Button variant="secondary">Secondary</Button>
            <Button variant="success">Success</Button>
            <Button variant="danger">Danger</Button>
            `,
        },
      },
    }}
  >
    <div
      style={{
        display: "flex",
        gap: "1rem",
        alignItems: "center",
        flexWrap: "wrap",
      }}
    >
      <Button variant="primary">Primary</Button>
      <Button variant="secondary">Secondary</Button>
      <Button variant="success">Success</Button>
      <Button variant="danger">Danger</Button>
    </div>
  </Story>
</Canvas>

| State                | Description                                                                                                                                                                                    |
| -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Primary button       | Use a Primary button to inform about the importance of an action. This is the strongest call to action. No more than one primary button should appear on a screen. Examples: Save, Finish, ... |
| Success button       | Use a Success button for reassurance. Use it preferably to indicate a validation, a success, an end of a workflow, ... Examples: Confirm, Save, ...                                            |
| Danger button        | Use a Danger button to indicate a destructive action. Example: Delete |


### Button with icon

To mark a button as with icon, add the `startIcon` `endIcon` property to the `Button`.

<Canvas withSource="open">
  <Story
    name="icon"
    parameters={{
      docs: {
        source: {
          code: `
            <Button startIcon='info' variant='danger'>Button with start icon</Button>
            <Button endIcon='calendar'>Button with end icon</Button>
            <Button onlyIcon='plus' />
            `,
        },
      },
    }}
  >
    <div
      style={{
        display: "flex",
        gap: "1rem",
        alignItems: "center",
        flexWrap: "wrap",
      }}
    >
      <Button startIcon='info' variant='danger'>Button with start icon</Button>
      <Button endIcon='calendar'>Button with end icon</Button>
      <Button onlyIcon='plus' />
    </div>
  </Story>
</Canvas>

### Disabled

To mark a button as disabled, add the `disabled` property to the `Button`.

<Canvas withSource="open">
  <Story name="disabled">
    <Button disabled>Disabled Button</Button>
  </Story>
</Canvas>

## Props

<ArgsTable of={Button} />